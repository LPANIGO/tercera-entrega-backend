{{#each products}}
        <div class="productCard">

            <p>Nombre: {{name}}</p>
            <p>Descripción: {{description}}</p>
            <p>Precio: ${{price}}</p>
            <p>Stock: {{stock}}</p>
            <p>Imagen: </p>
            <img src="http://localhost:8080/img/{{this.thumbnail}}" alt="{{this.thumbnail}}">
        </div>
{{/each}}